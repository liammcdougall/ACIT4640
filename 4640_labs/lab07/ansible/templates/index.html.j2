<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Static Website</title>
</head>
<body>
    <h1>Static Website</h1>
    <p><strong>Inventory Hostname:</strong> {{ inventory_hostname }}</p>
    <p><strong>Ansible Groups:</strong> {{ group_names | join(', ') }}</p>
    <p><strong>Public DNS Name:</strong> {{ ansible_facts.ec2_public_hostname }}</p>
    <p><strong>EC2 Hostname:</strong> {{ inventory_hostname }}</p>
    <p><strong>Private IP Address:</strong> {{ ansible_facts.default_ipv4.address }}</p>
    <p><strong>Public IP Address:</strong> {{ ansible_facts.ec2_public_ipv4 }}</p>
</body>
</html>
